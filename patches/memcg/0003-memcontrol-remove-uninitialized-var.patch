From f02a4835b3238c19312f9b26fec33b4a51d5176c Mon Sep 17 00:00:00 2001
From: otaviomorais <otaviomorais@users.noreply.github.com>
Date: Mon, 10 Aug 2026 17:58:32 -0300
Subject: [PATCH] fix(memcg): drop uninitialized_var() (removed from grafted
 5.10 headers)

The macro uninitialized_var() was removed upstream (v5.10); the e404 base
grafts 5.10-era compiler headers, so it is undefined. MEMCG was previously
disabled so memcontrol.c never compiled; enabling MEMCG exposed this. Plain
declaration is fine with modern Clang 20.
---
 mm/memcontrol.c | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/mm/memcontrol.c b/mm/memcontrol.c
index eba14a03f..f35fd8ce2 100644
--- a/mm/memcontrol.c
+++ b/mm/memcontrol.c
@@ -919,7 +919,7 @@ struct mem_cgroup *mem_cgroup_iter(struct mem_cgroup *root,
 				   struct mem_cgroup *prev,
 				   struct mem_cgroup_reclaim_cookie *reclaim)
 {
-	struct mem_cgroup_reclaim_iter *uninitialized_var(iter);
+	struct mem_cgroup_reclaim_iter *iter;
 	struct cgroup_subsys_state *css = NULL;
 	struct mem_cgroup *memcg = NULL;
 	struct mem_cgroup *pos = NULL;
-- 
2.55.0

